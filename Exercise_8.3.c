/* Using the shift left and shift right operator */

#include <stdio.h>
main()
{
	int x = 123;
	int y = 4;
	
	printf("x<<y will yield: %d\n", x<<y);
	printf("x>>y will yield: %d\n", x>>y);
	return 0;
}
